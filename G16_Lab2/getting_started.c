#include "address_map_arm.h"

/* This program determines the largest value in a list.*/

int main(){
	for (int i = 0; i < 5 ; i++){
		if (max_val < a[i]) {
			max_val = a[i];
		}
	}
	return max_val;

}

